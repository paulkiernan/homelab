---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: disconnect-2022
  namespace: disconnect
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-production
    external-dns.alpha.kubernetes.io/hostname: disconnect.paulynomial.com,disc.paulynomial.com
    traefik.ingress.kubernetes.io/router.middlewares: traefik-default-redirect-https@kubernetescrd
spec:
  ingressClassName: traefik
  rules:
    - host: "disconnect.paulynomial.com"
      http:
        paths:
          - backend:
              service:
                name: disconnect-2022
                port:
                  name: http
            path: /
            pathType: Prefix
    - host: "disc.paulynomial.com"
      http:
        paths:
          - backend:
              service:
                name: disconnect-2022
                port:
                  name: http
            path: /
            pathType: Prefix
  tls:
    - hosts:
        - disconnect.paulynomial.com
        - disc.paulynomial.com
      secretName: disconnect-paulynomial-com-tls
