# Arch Installation Pre-reqs

The following packages need to be installed before these script will work.

```bash
yay -S \
    aur/flux-bin \
    community/age \
    community/direnv \
    community/ipcalc
```
